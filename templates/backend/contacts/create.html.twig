{% extends 'backend/base.html.twig' %}

{% block body %}
    <h1>Edit contact</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }}

{% endblock %}

