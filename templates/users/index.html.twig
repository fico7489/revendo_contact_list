{% extends 'base.html.twig' %}

{% block body %}
    <h1>Test users</h1>

    {% for user in users %}
        {{ user.firstName }} {{ user.lastName }}, {{ user.email }}<br/>
    {% endfor %}
{% endblock %}
